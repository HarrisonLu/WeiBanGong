<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springapp.mvc.contacts.dao.UserMapper">
    <resultMap id="UserResult" type="User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="chineseName" column="chinese_name"/>
        <result property="englishName" column="english_name"/>
        <result property="positionTitle" column="position_title"/>
        <result property="weChatNum" column="wechat_num"/>
        <result property="qqNum" column="qq_num"/>
        <result property="phoneNum" column="phone_num"/>
        <result property="createdTime" column="created_time"/>
    </resultMap>
    
    <resultMap id="UserDepartmentResult" type="User" extends="UserResult">
        <collection property="departments" ofType="Department" columnPrefix="dp_">
            <id property="id" column="id"/>
            <result property="name" column="name"/>
            <result property="createdTime" column="created_time"/>
        </collection>
    </resultMap>

    <insert id="insertUser" parameterType="User" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into t_user values(#{id},#{email},#{password},#{chineseName},#{englishName},#{positionTitle},#{weChatNum},#{qqNum},#{phoneNum},#{createdTime})
    </insert>

    <select id="selectUserById" parameterType="int" resultMap="UserDepartmentResult">
        select u.id,u.email,u.chinese_name,d.name as dp_name
        from t_user u
        left outer join t_user_department ud on u.id=ud.user_id
        left outer join t_department d on ud.department_id=d.id
        where id=#{id}
    </select>

    <select id="selectAllUser" resultMap="UserDepartmentResult">
        select u.id,u.email,u.chinese_name,d.name as dp_name
        from t_user u
        left outer join t_user_department ud on u.id=ud.user_id
        left outer join t_department d on ud.department_id=d.id
    </select>

    <select id="findUserByChineseName" parameterType="java.lang.String" resultType="User">
        select * from t_user where chinese_name = #{chineseName}
    </select>

    <select id="findUserByEnglishName" parameterType="java.lang.String" resultType="User">
        select * from t_user where english_name = #{englishName}
    </select>


    <update id="updateUser" parameterType="User">
        update t_user set
        email = #{email},
        password = #{password},
        chinese_name = #{chineseName},
        english_name = #{englishName},
        position_title = #{positionTitle},
        wechat_num = #{weChatNum},
        qq_num = #{qqNum},
        phone_num = #{phoneNum}
        where
        id = #{id}
    </update>

    <delete id="deleteUser" parameterType="User">
        delete from t_user where id = #{id}
    </delete>


</mapper>

