<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springapp.mvc.contacts.dao.DepartmentMapper">
    <resultMap id="Department" type="Department">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
    </resultMap>

    <insert id="insertDepartment" parameterType="Department" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
         insert into t_department values(#{id},#{name},#{createdTime})
    </insert>

    <select id="selectDepartmentById" parameterType="java.lang.Integer" resultMap="Department">
        select d.id,d.name,
        u.id as user_id,u.chinese_name as user_chinese_name,u.english_name as user_english_name
        from t_department d
        left outer join t_user_department ud on d.id=ud.department_id
        left outer join t_user u on ud.user_id=u.id
        where d.id=#{id}
    </select>

    <select id="selectDepartmentByName" parameterType="java.lang.String" resultMap="Department">
        select * from t_department where name=#{name}
    </select>

    <select id="selectAllDepartment" resultMap="Department">
        select * from t_department
    </select>

    <update id="updateDepartment" parameterType="Department">
        update t_department set
        name = #{name}
        where id = #{id}
    </update>

    <delete id="deleteDepartment" parameterType="java.lang.Integer">
        delete from t_department where id = #{id}
    </delete>

</mapper>